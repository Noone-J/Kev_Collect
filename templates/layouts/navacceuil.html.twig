<!-- Barre de navigation -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark custom-navbar">
  <!-- Conteneur pour le contenu de la barre de navigation -->
  <div class="container-fluid">
    <!-- Logo ou titre de la marque -->
    <a class="navbar-brand" href="">KevCollect</a>
    
    <!-- Bouton pour basculer la navigation sur les écrans plus petits -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
            data-bs-target="#navbarNav" 
            aria-controls="navbarNav" 
            aria-expanded="false" 
            aria-label="Basculement de navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <!-- Conteneur collapsible pour les liens de navigation -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Menu principal de navigation -->
      <ul class="navbar-nav me-auto">
        <!-- Lien actif (page actuelle) -->
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{path('app_acceuil')}}">Accueil</a>
        </li>
      </ul>
      
      <!-- Menu secondaire de navigation -->
      <ul class="navbar-nav">
        <!-- Lien pour se connecter -->
        <li class="nav-item">
          <a class="nav-link btn btn-success" href="{{ path('app_login') }}">Connection</a>
        </li>
      </ul>
    </div>
  </div>
</nav>